# UsageMonitor 安全说明（SECURITY.md）

> 本文档面向用户与安全研究者，说明 UsageMonitor 如何保护你的登录态与 API Key、程序的网络行为边界、
> 插件安全模型、已知的安全边界，以及漏洞披露方式。
> 我们的原则是：**只声明可被验证的承诺，并诚实说明保护不了什么。**

---

## 1. 我们保护什么：凭据存储

UsageMonitor 需要保存你的服务商登录态（Cookie）或 API Key 才能拉取用量数据。所有敏感凭据均按以下机制保护：

| 机制 | 说明 |
|---|---|
| **Windows DPAPI 加密（CurrentUser 域）** | 与 Windows 凭据管理器、浏览器保存密码同级别的系统加密。密文**换一台电脑、换一个 Windows 账户都无法解密**。Cookie 文件（`cookies/*.json`）与 `config.json` 中的敏感字段均加密落盘。 |
| **HMAC-SHA256 完整性签名** | Cookie 文件带完整性签名（格式 `magic + version + hmac + ciphertext`），被篡改或跨设备复制的文件会验签失败并要求重新登录。签名密钥随机生成、DPAPI 加密后独立存储。 |
| **NTFS 目录 ACL 收紧** | `%AppData%\UsageMonitor\`（含 `cookies/`）阻断权限继承，仅当前 Windows 用户有访问权。 |
| **敏感字段显式声明 + 自描述密文** | 插件通过配置字段元数据（`"sensitive": true` 或 Password 类型）显式声明凭据字段，非常规命名的凭据（如 SessionId）同样被加密；密文带 `enc:v1:` 前缀自描述，关键词表（apikey/token/secret/password/cookie）仅作兜底。 |
| **日志脱敏** | 错误响应体在写日志前经敏感模式脱敏（`api_key/authorization/token/secret` → `***REDACTED***`）并截断；凭据明文绝不写入日志。 |

## 2. 网络行为边界（可验证声明）

UsageMonitor 的全部出站请求仅限于：

1. **各插件声明包中声明的服务商域名**（登录页、用量接口）；
2. **服务商 favicon 抓取**（用于显示图标，按登录域名请求）。

**没有遥测、没有统计上报、没有任何第三方数据上传。**

这不是口头承诺，而是有强制机制的可验证声明：

- **凭据域名同源约束**：携带 Cookie / 敏感配置占位符的取数请求，目标域名必须命中声明包
  声明的官方域集合（`loginConfig` / `fetch.capture` / `usageUrls` / `credentialDomains`），
  否则宿主直接拒绝发送并记录告警日志——即使插件声明包被恶意篡改，你的登录态也无法被发往陌生域名。
- **SSRF 防护**：所有声明式 HTTP 请求强制 HTTPS，且禁止环回 / 内网 / 链路本地地址。
- **验证方法**：欢迎使用 Fiddler / Wireshark / Proxifier 等工具抓包核对出站域名；
  每个插件的官方域声明可直接在 `plugins/<包名>/*.json` 中查看（纯 JSON，可读可审计）。

## 3. 插件安全模型

- **声明包 = 纯 JSON，不可执行代码**。新增服务商只需 JSON 声明，宿主是唯一执行体；
  加载时经 `PluginValidator` 白名单校验（字段名、图表类型、https 强制、凭据域名静态预校）。
- **DLL 插件通道默认关闭**；即使显式开启，也要求 SHA256 哈希白名单命中才会加载。
- 安装第三方声明包前，建议运行 `UsageMonitor.exe --validate-plugin <目录>` 或在设置界面点击「校验插件」。

## 4. 诚实的边界：我们保护不了什么

- **已在你的 Windows 账户下运行的恶意软件**。DPAPI CurrentUser 域允许同用户进程解密——这是包括浏览器
  在内的所有 Windows 桌面应用的共同天花板。请保持系统与杀毒软件更新。
- **进程内存**。程序运行期间，解密后的凭据存在于进程内存中，具备调试/转储权限的攻击者可提取。
- **你主动录入给某插件的 API Key**：Key 的使用范围以该插件声明的官方域为界；请只安装可信来源的声明包。

## 5. 漏洞披露

如果你发现安全漏洞：

- 请**不要**公开提交 Issue，先通过仓库 README 中的联系方式私下报告；
- 我们承诺在 7 天内响应、90 天内修复或给出说明；
- 欢迎附带复现步骤与影响面分析，修复后将在版本说明中致谢（除非你要求匿名）。

## 6. 版本与校验

- 正式发布版将提供 SHA256 校验值；下载后请核对。
- 敏感文件（`config.json`、`cookies/`、`secrets/`、日志）均被 `.gitignore` 排除，不会出现在仓库中。
