<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <RootNamespace>UsageMonitor.App</RootNamespace>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <!-- 项目版本号（0.39.0-beta）：TooltipDisplaySettings新增(+13行) / AreaChartControl大幅增强(+88行) / MiniLineChartControl增强(+47行) / SettingsWindow.xaml.cs增强(+69行) / MiniChartItemViewModel增强(+48行) / MainViewModel增强(+38行) / ProviderUsageViewModel增强(+40行) / StackedBarChartControl增强(+28行) / 核心数据模型(CardDeclaration/ChartData/UsageFields)更新 / csproj图标资源分发策略调整；用于程序集元数据 + 文件属性。 -->
    <!-- 显式设置 5 个属性，避免被 SDK 衍生规则变动影响。 -->
    <Version>0.39.0-beta</Version>
    <AssemblyVersion>0.39.0.0</AssemblyVersion>
    <FileVersion>0.39.0.0</FileVersion>
    <InformationalVersion>0.39.0-beta</InformationalVersion>
    <ProductVersion>0.39.0-beta</ProductVersion>
    <AssemblyTitle>UsageMonitor</AssemblyTitle>
    <Product>UsageMonitor</Product>
    <Company>UsageMonitor</Company>
    <Copyright>Copyright © 2026</Copyright>
    <!-- req-055：DPI感知manifest，支持多显示器PerMonitorV2 -->
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <!-- Logo v3：exe 可执行文件图标（多尺寸 ico，透明背景 + #6fc4c7 前景） -->
    <ApplicationIcon>Assets\Providers\UsageMonitor-v3.ico</ApplicationIcon>
    <!-- req-086-3.4：App 层作为兼容层继续使用旧字段，抑制 CS0618 警告 -->
    <!-- req-055：app.manifest 有意声明 PerMonitorV2 DPI，抑制 WinForms 分析器 WFAC010 -->
    <NoWarn>$(NoWarn);CS0618;WFAC010</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\UsageMonitor.Core\UsageMonitor.Core.csproj" />
  </ItemGroup>

  <!--
    Stage F（完全声明式插件架构）：宿主零内置 Provider。
    MiniMax 纯声明包（仅 defaults.json，零 DLL）随构建复制到输出目录
    plugins/UsageMonitor.Plugin.MiniMax/，由 PluginManager 声明包扫描加载。
  -->
  <ItemGroup>
    <Content Include="..\Plugins\UsageMonitor.Plugin.MiniMax\defaults.json"
             Link="plugins\UsageMonitor.Plugin.MiniMax\defaults.json"
             CopyToOutputDirectory="PreserveNewest" />
    <!-- req-116：插件语言包（i18n/<lang>.json）随声明包一起复制 -->
    <Content Include="..\Plugins\UsageMonitor.Plugin.MiniMax\i18n\*.json"
             Link="plugins\UsageMonitor.Plugin.MiniMax\i18n\%(Filename)%(Extension)"
             CopyToOutputDirectory="PreserveNewest" />
    <!-- DeepSeek 纯声明包（API 计费型：余额/消费/请求/Token 图表） -->
    <Content Include="..\Plugins\UsageMonitor.Plugin.DeepSeek\defaults.json"
             Link="plugins\UsageMonitor.Plugin.DeepSeek\defaults.json"
             CopyToOutputDirectory="PreserveNewest" />
    <Content Include="..\Plugins\UsageMonitor.Plugin.DeepSeek\i18n\*.json"
             Link="plugins\UsageMonitor.Plugin.DeepSeek\i18n\%(Filename)%(Extension)"
             CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="8.0.1" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.0" />
  </ItemGroup>

  <!--
    图标资源（分发准备）：
    - 第三方服务商品牌 Logo 不随包分发（规避商标再分发风险），改由 ProviderIconService
      在首次运行时按服务商域名抓取 favicon，缓存到 %AppData%/UsageMonitor/icons。
    - 仅随包分发本项目自有 Logo（usage-monitor-logo.png / UsageMonitor-v3.ico）。
  -->
  <ItemGroup>
    <Content Include="Assets\Providers\usage-monitor-logo.png">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <!-- Logo v3：多尺寸 ico 随包分发，供托盘 NotifyIcon 直接加载（高分屏清晰） -->
    <Content Include="Assets\Providers\UsageMonitor-v3.ico">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

</Project>
